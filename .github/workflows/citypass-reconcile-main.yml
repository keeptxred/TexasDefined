name: CityPASS reconcile main

on:
  push:
    branches:
      - feature/citypass-texas-evergreen
    paths:
      - .github/workflows/citypass-reconcile-main.yml

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/citypass-texas-evergreen
          fetch-depth: 0
      - name: Merge current main without weakening safeguards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
          git push origin HEAD:feature/citypass-texas-evergreen
