name: Retired — King County certification audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit-retired-certification:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify retained King County certification
        run: node scripts/ci/verify-retired-county-certification.mjs king
