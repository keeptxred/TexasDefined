name: Certify Borden County production

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: write
  pull-requests: write

jobs:
  certify:
    uses: ./.github/workflows/certify-county-production-reusable.yml
    with:
      county: borden
    secrets: inherit
