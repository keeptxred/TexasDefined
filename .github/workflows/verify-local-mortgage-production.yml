name: Verify consolidated mortgage production

on:
  pull_request:
    paths:
      - '.github/workflows/verify-local-mortgage-production.yml'
      - 'scripts/ci/verify-local-mortgage-pages.mjs'
      - 'scripts/data/validate-local-mortgage-seo.mjs'
      - 'src/components/calculators/CalculatorPage.tsx'
      - 'src/components/calculators/ConsolidatedLocationSelector.tsx'
      - 'src/data/local-home-affordability.ts'
      - 'src/data/local-mortgage.ts'
      - 'src/data/sitemap-dependencies.server.ts'
      - 'src/routes/texas-mortgage-calculator*.tsx'
      - 'src/routes/sitemap*.ts'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/verify-local-mortgage-production.yml'
      - 'scripts/ci/verify-local-mortgage-pages.mjs'
      - 'scripts/data/validate-local-mortgage-seo.mjs'
      - 'src/components/calculators/CalculatorPage.tsx'
      - 'src/components/calculators/ConsolidatedLocationSelector.tsx'
      - 'src/data/local-home-affordability.ts'
      - 'src/data/local-mortgage.ts'
      - 'src/data/sitemap-dependencies.server.ts'
      - 'src/routes/texas-mortgage-calculator*.tsx'
      - 'src/routes/sitemap*.ts'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: local-mortgage-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-contract:
    name: Validate consolidated mortgage authority
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Validate preserved local context and redirect contract
        run: node scripts/data/validate-local-mortgage-seo.mjs

  verify-production:
    name: Verify canonical mortgage page and 19 legacy redirects
    if: ${{ github.event_name != 'pull_request' }}
    needs: validate-contract
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify canonical page, permanent redirects and sitemap retirement
        run: node scripts/ci/verify-local-mortgage-pages.mjs
