# Current-main bundle baseline audit

Diagnostic-only marker used to run the protected validation workflow against an otherwise unchanged current `main` tree. No runtime, validator, budget, indexing, publication, or deployment behavior is changed.
