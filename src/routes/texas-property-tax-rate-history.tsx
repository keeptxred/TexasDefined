import{createFileRoute,Link}from'@tanstack/react-router';import{useMemo,useState}from'react';import{texasDefinedBrand}from'@/brand/texasdefined';import{TaxingUnitSearch}from'@/components/property/TaxingUnitSearch';import{Container}from'@/components/layout/Container';import{absoluteUrl,buildMeta,canonicalLink,jsonLd}from'@/lib/seo';import type{TexasTaxRateRecord}from'@/data/property/texas-tax-rates.generated';
const canonicalPath='/texas-property-tax-rate-history';const description='Explore annual Texas property-tax rates for counties, cities, school districts and special districts using historical Comptroller statewide files.';const pageUrl=absoluteUrl(texasDefinedBrand,canonicalPath);export const Route=createFileRoute('/texas-property-tax-rate-history')({head:()=>({meta:buildMeta(texasDefinedBrand,{canonicalPath,title:'Texas Property Tax Rate History Explorer',description}),links:[canonicalLink(texasDefinedBrand,canonicalPath)],scripts:[jsonLd({'@context':'https://schema.org','@type':'WebApplication','@id':`${pageUrl}#tool`,name:'Texas Property Tax Rate History Explorer',description,url:pageUrl,applicationCategory:'FinanceApplication',operatingSystem:'Any'})]}),component:Page});
function displayRate(record:TexasTaxRateRecord|undefined){if(!record)return'—';if(record.totalRate!=null&&!record.variableRate)return record.totalRate.toFixed(6);return record.rateVariants.length?`Varies: ${record.rateVariants.map(rate=>rate.toFixed(6)).join(' / ')}`:'Verify parcel'}
function Page(){const[selected,setSelected]=useState<TexasTaxRateRecord|null>(null);const[history,setHistory]=useState<TexasTaxRateRecord[]>([]);const[status,setStatus]=useState('');async function choose(record:TexasTaxRateRecord){setSelected(record);setStatus('Loading rate history…');try{const response=await fetch(`/api/property-tax-rates?unit=${encodeURIComponent(record.slug)}&type=${encodeURIComponent(record.type)}`);const body=await response.json() as{history?:TexasTaxRateRecord[];error?:string};if(!response.ok)throw new Error(body.error||`History lookup failed (${response.status})`);setHistory(body.history??[]);setStatus('')}catch(error){setHistory([]);setStatus(error instanceof Error?error.message:'History lookup failed.')}}const numericRates=useMemo(()=>history.flatMap(item=>item.totalRate!=null&&!item.variableRate?[item.totalRate]:item.rateVariants),[history]);const max=Math.max(1,...numericRates);const first=history[0],last=history.at(-1);const change=first?.totalRate!=null&&!first.variableRate&&last?.totalRate!=null&&!last.variableRate?last.totalRate-first.totalRate:null;return <Container className="pb-16 pt-12 sm:pb-24 sm:pt-16"><article className="mx-auto max-w-6xl"><nav className="border-b border-border pb-4 text-xs uppercase tracking-[.14em] text-muted-foreground"><Link to="/property">Property</Link><span className="mx-2">/</span><Link to="/property-tax-calculators">Calculators</Link><span className="mx-2">/</span>Rate history</nav><header className="border-b border-border py-10"><p className="eyebrow text-primary">Historical tax data</p><h1 className="mt-3 font-display text-5xl sm:text-7xl">Texas property tax rate history explorer</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-muted-foreground">Search a county, city, ISD, MUD or other special district and compare its finalized annual rates across the statewide Comptroller files retained by TexasDefined.</p></header><section className="grid gap-8 py-10 lg:grid-cols-[15rem_1fr]"><div><p className="eyebrow text-primary">Taxing unit</p><h2 className="mt-2 font-display text-3xl">Find a local rate</h2></div><div><TaxingUnitSearch allowVariableSelection onSelect={record=>void choose(record)}/>{selected?<p className="mt-4 text-sm"><strong>{selected.name}</strong> · {selected.type.replaceAll('-',' ')}</p>:null}{status?<p className="mt-3 text-sm text-muted-foreground">{status}</p>:null}</div></section>{history.length?<section className="border-y border-border py-10"><div className="grid gap-6 md:grid-cols-3"><Fact label="First year" value={`${first?.year}: ${displayRate(first)}`}/><Fact label="Latest year" value={`${last?.year}: ${displayRate(last)}`}/><Fact label="Net fixed-rate change" value={change==null?'Not comparable across variable years':`${change>=0?'+':''}${change.toFixed(6)}`}/></div><div className="mt-8 space-y-4">{history.map(item=>{const chartRate=item.totalRate??Math.max(0,...item.rateVariants);return <div key={item.id}><div className="mb-1 flex items-center justify-between gap-4 text-sm"><span>{item.year}</span><strong>{displayRate(item)} per $100</strong></div><div className="h-2 bg-muted"><div className="h-full bg-primary" style={{width:`${Math.max(2,chartRate/max*100)}%`}}/></div></div>})}</div><div className="mt-8 overflow-x-auto"><table className="w-full min-w-[42rem] text-left text-sm"><thead><tr className="border-b border-border"><th className="py-3">Year</th><th>Tax rate</th><th>M&O</th><th>Debt / I&S</th><th>Reported levy</th></tr></thead><tbody>{history.map(item=><tr key={`${item.id}-row`} className="border-b border-border"><td className="py-3">{item.year}</td><td>{displayRate(item)}</td><td>{item.maintenanceOperationsRate?.toFixed(6)??'—'}</td><td>{item.debtServiceRate?.toFixed(6)??'—'}</td><td>{item.levy!=null?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(item.levy):'—'}</td></tr>)}</tbody></table></div></section>:null}<p className="mt-8 text-sm leading-6 text-muted-foreground">Historical rates are reported statewide snapshots. A rate history does not prove that a particular parcel was inside the taxing unit in every year shown. “Varies” means the official source contains multiple rates or a parcel/school-area qualifier; TexasDefined does not collapse those into a false single rate.</p></article></Container>}
function Fact({label,value}:{label:string;value:string}){return <div className="border-t border-border pt-3"><span className="text-xs uppercase tracking-[.12em] text-muted-foreground">{label}</span><strong className="mt-1 block font-display text-2xl">{value}</strong></div>}
