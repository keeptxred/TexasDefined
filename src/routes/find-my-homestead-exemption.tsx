import { createFileRoute } from '@tanstack/react-router'; import { buildMeta } from '@/lib/seo'; import { LazyRelocationServiceFinder } from '@/components/relocation/LazyRelocationServiceFinder';
export const Route=createFileRoute('/find-my-homestead-exemption')({head:()=>({meta:buildMeta('/find-my-homestead-exemption',{title:'Find Where to File a Texas Homestead Exemption',description:'Find county context for a Texas residence-homestead exemption and verify filing instructions with the responsible appraisal district.'})}),component:()=> <LazyRelocationServiceFinder kind="homestead"/>});
