import { createFileRoute } from '@tanstack/react-router'; import { buildMeta } from '@/lib/seo'; import { LazyRelocationServiceFinder } from '@/components/relocation/LazyRelocationServiceFinder';
export const Route=createFileRoute('/find-my-property-tax')({head:()=>({meta:buildMeta('/find-my-property-tax',{title:'Find Your Texas Property Tax and Appraisal Offices',description:'Find county context for Texas appraisal-district and property-tax office research, then verify the parcel with official local sources.'})}),component:()=> <LazyRelocationServiceFinder kind="property-tax"/>});
